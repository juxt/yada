The __response body__ has a __:body__ entry with a function value.

<resource-map/>

When a request is received, the function is called and the result becomes the value of the response map's __:body__ entry.

<response/>
